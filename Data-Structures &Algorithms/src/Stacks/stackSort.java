package Stacks;

public class stackSort {

	public static void sort(StackObj s) {
		StackObj s2= new StackObj(s.size());
		StackObj s3= new StackObj(s.size());
 
		for(int i=1;i<s.size();i++) {
			int value=(int) s.top();
			
		}
	}
}
