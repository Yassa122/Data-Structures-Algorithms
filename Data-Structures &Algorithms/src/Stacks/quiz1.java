/*
 * package Stacks;
 * 
 * public class quiz1 { public static ArrayStack crash(int[]arr) { ArrayStack
 * s=new ArrayStack(arr.length); int left; int right; for(int
 * i=0;i<arr.length;i++) { if(arr[i]>0) { right=arr[i]; s.push(arr[i]); }else {
 * left=arr[i]; s.push(arr[i]); } while(!s.isEmpty()) {
 * if(left==right||left<right) { s.pop(); } }
 * 
 * } } }
 */